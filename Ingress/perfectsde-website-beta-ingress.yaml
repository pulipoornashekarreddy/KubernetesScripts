apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: perfect-sde-website-beta-ingress
  namespace: perfect-sde-beta
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: beta.perfectsde.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: perfect-sde-content-website-beta
                port:
                  number: 80